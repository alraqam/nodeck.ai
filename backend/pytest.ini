[pytest]
testpaths = tests
# Tests import `app.*`, so the backend root must be importable without an
# editable install.
pythonpath = .
addopts = -q --strict-markers
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
